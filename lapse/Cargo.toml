[package]
name = "lapse"
version = "0.1.0"
edition = "2024"

[dependencies]
http = "1.4.2"
mlua = { version = "0.12.0", features = ["lua54", "vendored", "async"] }
reqwest = { version = "0.13.4", features = ["multipart", "stream", "cookies"] }
serde = { workspace = true }
serde_json = "1.0.150"
stream = "0.1.0"
thiserror = {workspace = true}

[dev-dependencies]
tempfile = {workspace = true}
