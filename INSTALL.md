# CLI

## Using cargo

Works for every major OS, as long as you have cargo installed: https://rustup.rs/

```bash
cargo install lapse-cli
```

## Manual installation

On releases tab, download the binary that runs on your OS (exe for Windows and the binary for Linux), and then add the binary to the PATH.
